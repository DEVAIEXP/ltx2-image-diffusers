# DDO WSL/Linux benchmark commands
#
# Runner: run_dynamic_modular_distilled.py
# Benchmark: LTX 2.3 image BF16, 1280x704, 8 steps, seed 43, real prompt.
# Goal: keep these equivalent to the Windows matrix.
#
# Notes:
# - Do not set Windows standby purge variables on WSL/Linux.
# - DDO_PRESET=auto resolves through the library.
# - DDO_RUNNER_METRICS_LEVEL=2 prints minimal metrics and saves metrics JSON.
# - Run each block in a fresh shell when possible.


###############################################################################
# WSL Ubuntu
###############################################################################

# 0. Base directory
cd /mnt/e/ProjetosIA/ltx2.3-image-diffusers


# 1. DDO auto / one_shot_fast
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=auto
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 2. DDO low RAM safe
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=low_ram_safe
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 3. Official Diffusers block-level group offload baseline
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=diffusers_offload_compat
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 4. Official Diffusers leaf-level group offload baseline
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=diffusers_leaf_offload_compat
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 5. Optional: WSL explicit compatibility preset
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=wsl_compat
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


###############################################################################
# Native Ubuntu/Linux
###############################################################################

# 0. Base directory
# Change this path to wherever the repo is mounted on native Linux.
cd /path/to/ltx2.3-image-diffusers


# 1. DDO auto / one_shot_fast
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=auto
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 2. DDO low RAM safe
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=low_ram_safe
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 3. Official Diffusers block-level group offload baseline
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=diffusers_offload_compat
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# 4. Official Diffusers leaf-level group offload baseline
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=diffusers_leaf_offload_compat
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


###############################################################################
# Optional stress checks, only after the main matrix above
###############################################################################

# A. Simulate a 32 GB system RAM decision on native Linux or WSL.
# This validates whether the planner skips pinned CPU weights when RAM is tight.
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=auto
export DDO_AVAILABLE_SYSTEM_RAM_GB=32
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=1
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py


# B. Warm process check.
# This measures server-like behavior where some DDO process-lifetime caches can matter.
for v in $(env | awk -F= '/^DDO_/ {print $1}'); do unset "$v"; done
export DDO_PRESET=warm_process
export DDO_RUNNER_WIDTH=1280
export DDO_RUNNER_HEIGHT=704
export DDO_RUNNER_STEPS=8
export DDO_RUNNER_SEED=43
export DDO_RUNNER_GENERATION_REPEATS=2
export DDO_RUNNER_TRANSFORMER_PREPARE_REPEATS=2
export DDO_RUNNER_METRICS_LEVEL=2
python run_dynamic_modular_distilled.py
